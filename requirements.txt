# --- API katmani ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3

# --- Ajan orkestrasyonu (coklu ajan + insan onayi) ---
langgraph>=1.0
# Bekleyen onaylar sunucu yeniden baslasa da kaybolmasin diye kalici checkpoint
langgraph-checkpoint-sqlite>=3.0
aiosqlite>=0.20

# --- LLM: bulut (Gemini) + yerel (Ollama) ---
google-genai>=1.0
ollama>=0.4

# --- RAG / Vektor DB ---
qdrant-client[fastembed]>=1.10
pypdf>=4.2

# --- Loglama / SQL ---
SQLAlchemy>=2.0
